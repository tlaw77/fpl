name: Simulation Stability

on:
  workflow_run:
    workflows: ["FPL ETL"]
    types: [completed]
  workflow_dispatch:
  push:
    paths:
      - "simulation_stability.py"
      - "chip_activation_gate.py"
      - "triple_captain_review.py"
      - "captaincy_review.py"
      - ".github/workflows/simulation-stability.yml"

concurrency:
  group: fpl-simulation-stability
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  summarize:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout latest main
        uses: actions/checkout@v4
        with:
          ref: main
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build stability history
        run: python simulation_stability.py
      - name: Build chip activation gate
        run: python chip_activation_gate.py
      - name: Build owned-squad Triple Captain review
        run: python triple_captain_review.py
      - name: Build dedicated captaincy review
        run: python captaincy_review.py
      - name: Validate supplemental outputs
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          data = json.loads(Path('data/simulation_stability.json').read_text())
          gate = json.loads(Path('data/chip_activation_gate.json').read_text())
          tc = json.loads(Path('data/triple_captain_review.json').read_text())
          cap = json.loads(Path('data/captaincy_review.json').read_text())
          assert data['status'] == 'SUCCESS'
          assert data.get('version', 0) >= 2
          assert data.get('snapshot_count', 0) >= 1
          assert data.get('snapshot_count', 0) <= 192
          summary = data.get('summary') or {}
          assert summary.get('window_runs', 0) >= 1
          assert float(summary.get('effective_evidence_runs', 0)) > 0
          assert 0 <= float(summary.get('action_persistence_pct', 0)) <= 100
          assert 0 <= float(summary.get('leader_persistence_pct', 0)) <= 100
          assert gate['status'] == 'SUCCESS'
          for key in ('wildcard','free_hit','bench_boost','triple_captain'):
              assert gate[key]['status'] in ('HOLD','WATCH','CONSIDER')
          assert tc['status'] == 'SUCCESS'
          assert tc.get('version', 0) >= 1
          assert tc.get('decision', {}).get('status') in ('HOLD','WATCH','CONSIDER')
          assert len(tc.get('current_gw_shortlist', [])) >= 1
          assert cap['status'] == 'SUCCESS'
          assert cap.get('version', 0) >= 1
          assert cap.get('captain') is not None
          assert len(cap.get('shortlist', [])) >= 2
          assert 0 <= int(cap.get('confidence', 0)) <= 100
          print('Simulation stability, chip gate, TC review and captaincy review validation passed')
          PY
      - name: Commit supplemental snapshots
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/simulation_stability.json data/chip_activation_gate.json data/triple_captain_review.json data/captaincy_review.json
          if git diff --cached --quiet; then
            echo "No supplemental changes"
          else
            git commit -m "Update simulation stability chip and captaincy reviews"
            git pull --rebase origin main
            git push origin main
          fi
