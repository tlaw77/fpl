(()=>{
const BUILD='safe-transfer-routes-stage17-20260827-2218';
const DATA='https://raw.githubusercontent.com/tlaw77/fpl/main/data/latest.json';
const POOL='https://raw.githubusercontent.com/tlaw77/fpl/main/data/player_pool.json';
const KEY='fplWorkingPlanV2';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[c]));
const n=(v,d=0)=>Number.isFinite(Number(v))?Number(v):d;
let loaded=false,latest=null,pool=null;
function currentPlan(){try{return JSON.parse(localStorage.getItem(KEY)||'null')}catch{return null}}
function part(m,type='model'){return{out:m.out||{},incoming:m.safe_in||m.in||m.aggressive_in||{},safe:n(m.safe_gain??m.score_improvement),lev:n(m.aggressive_gain??m.leverage_gain??m.score_improvement),type}}
function clean(p){return{player_id:Number(p?.player_id)||null,player:String(p?.player||''),club:String(p?.club||''),position:String(p?.position||''),price:Number.isFinite(Number(p?.price))?Number(p.price):null}}
function squadIds(d){return new Set((d.squad_next5||d.squad||[]).map(p=>Number(p.player_id)))}
function fallbackRows(d,p,base){const out=base[0]?.out||null;if(!out||!out.position)return[];const bank=n(d.me?.bank??d.current_bank),budget=n(out.price)+bank,ids=squadIds(d);const candidates=(p?.players||[]).filter(x=>x.position===out.position&&!ids.has(Number(x.player_id))&&n(x.price)<=budget&&n(x.availability,1)>=.75).map(x=>({out,incoming:x,safe:n(x.six_gw_score),lev:n(x.six_gw_score)+(40-n(x.effective_ownership_pct??x.mini_league_ownership_pct??0))*.04,type:'pool'})).sort((a,b)=>b.lev-a.lev);return candidates.slice(0,8)}
function collect(d,p){const dec=d.current_next_gw_decisions||d.next_gw_decisions||{};const src=[...(dec.safe_moves||[]).map(m=>part(m,'safe')),...(dec.safe_transfer_moves||[]).map(m=>part(m,'safe')),...(dec.aggressive_moves||[]).map(m=>part(m,'aggressive')),...(dec.aggressive_transfer_moves||[]).map(m=>part(m,'aggressive'))];const seen=new Set(),rows=[];for(const r of src){if(!r.out?.player||!r.incoming?.player)continue;const k=`${r.out.player_id||r.out.player}>${r.incoming.player_id||r.incoming.player}`;if(seen.has(k))continue;seen.add(k);rows.push(r)}for(const r of fallbackRows(d,p,rows)){const k=`${r.out.player_id||r.out.player}>${r.incoming.player_id||r.incoming.player}`;if(seen.has(k))continue;seen.add(k);rows.push(r)}return rows.slice(0,6)}
function isChosen(r){const m=currentPlan()?.moves?.[0];return !!m&&Number(m.out?.player_id)===Number(r.out?.player_id)&&Number(m.in?.player_id)===Number(r.incoming?.player_id)}
function save(r,btn){const obj={version:'safe-manual-v2',updated_at:new Date().toISOString(),moves:[{out:clean(r.out),in:clean(r.incoming),status:'committed',committed_at:new Date().toISOString()}]};try{localStorage.setItem(KEY,JSON.stringify(obj));document.querySelectorAll('[data-transfer-choice]').forEach(b=>{b.textContent='Choose this route';b.dataset.selected='0';b.style.background='#172033';b.style.color='#cbd5e1';b.style.borderColor='#475569'});btn.textContent='✓ Your choice';btn.dataset.selected='1';btn.style.background='#143126';btn.style.color='#6ee7b7';btn.style.borderColor='#34d39966';window.dispatchEvent(new CustomEvent('fplChoiceChanged',{detail:obj}));}catch(e){btn.textContent='Could not save'}}
function render(d,p){const host=document.getElementById('dc-transfer-view');if(!host)return;host.querySelector('[data-safe-routes]')?.remove();const rows=collect(d,p);if(!rows.length)return;const sec=document.createElement('section');sec.className='dc-card';sec.dataset.safeRoutes='1';sec.innerHTML=`<p class="eyebrow">TRANSFER OPTIONS</p><h3>${rows.length} viable routes to compare</h3><p class="subtle">The first route is the model's preferred move. Additional options include aggressive routes and, where the backend only supplies one move, the strongest affordable same-position candidates from the Player Pool.</p><div class="dc-player-list" style="margin-top:10px">${rows.map((r,i)=>{const chosen=isChosen(r),own=n(r.incoming.target_rival_ownership_pct??r.incoming.mini_league_ownership_pct??r.incoming.effective_ownership_pct),c=i===0?'#34d399':r.type==='pool'?'#fbbf24':'#60a5fa';const tag=i===0?'Preferred':r.type==='aggressive'?'Aggressive':'Alternative';return `<div class="dc-player" style="border-left:4px solid ${c};padding-left:12px;display:block"><div style="display:flex;justify-content:space-between;gap:8px"><div><strong>${tag} · ${esc(r.out.player||'—')} → ${esc(r.incoming.player||'—')}</strong><span class="dc-role">${esc(r.incoming.position||'')}</span></div><span>${esc(r.incoming.club||'')} · £${n(r.incoming.price).toFixed(1)}m</span></div><div style="font-size:10px;color:#9fb0c8;margin-top:5px">${r.type==='pool'?`6-GW score ${n(r.incoming.six_gw_score).toFixed(1)} · EO ${own.toFixed(0)}% · affordable fallback`:`lower-var ${r.safe.toFixed(1)} · leverage ${r.lev.toFixed(1)} · rival own ${own.toFixed(0)}%`}</div><button data-transfer-choice="${i}" type="button" style="margin-top:8px;padding:8px 10px;border-radius:9px;border:1px solid ${chosen?'#34d39966':'#475569'};background:${chosen?'#143126':'#172033'};color:${chosen?'#6ee7b7':'#cbd5e1'};font-weight:800">${chosen?'✓ Your choice':'Choose this route'}</button></div>`}).join('')}</div><p class="subtle" style="margin-top:9px">Choice is saved only when you tap. The working selection is then projected into Pick Team, Squad Shape, Player Pool and League Intel.</p>`;const editor=host.querySelector('#safe-plan-editor'),journal=document.getElementById('decision-history-panel');if(editor)editor.insertAdjacentElement('beforebegin',sec);else if(journal)journal.insertAdjacentElement('beforebegin',sec);else host.appendChild(sec);sec.querySelectorAll('[data-transfer-choice]').forEach((btn,i)=>btn.addEventListener('click',()=>save(rows[i],btn)));document.documentElement.dataset.safeRoutesBuild=BUILD}
async function load(){if(loaded)return;loaded=true;try{const ctl=new AbortController(),t=setTimeout(()=>ctl.abort(),9000);const [a,b]=await Promise.all([fetch(`${DATA}?route17=${Date.now()}`,{cache:'no-store',signal:ctl.signal}),fetch(`${POOL}?route17=${Date.now()}`,{cache:'no-store',signal:ctl.signal})]);clearTimeout(t);if(a.ok)latest=await a.json();if(b.ok)pool=await b.json();if(latest)render(latest,pool)}catch{}}
function bind(){document.querySelector('#decision-nav button[data-view="transfer"]')?.addEventListener('click',()=>{if(latest)render(latest,pool);else load()},{passive:true});setTimeout(load,500)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();