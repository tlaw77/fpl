(()=>{
const BUILD='press-conference-stage106-20260904-1255';
const URL='https://raw.githubusercontent.com/tlaw77/fpl/main/data/press_conference_watch.json';
const q=(s,r=document)=>r.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[c]));
let loading=null,data=null;
function age(ts){const t=new Date(ts||0).getTime();if(!Number.isFinite(t))return'—';const m=Math.max(0,Math.round((Date.now()-t)/60000));if(m<1)return'just now';if(m<60)return`${m}m ago`;return`${Math.floor(m/60)}h ago`}
function tone(sig){return sig==='OUT'?'bad':sig==='DOUBT'?'bad':sig==='MINUTES'?'watch':sig==='FIT'?'good':'neutral'}
function action(p){if(p.strongest_signal==='OUT')return'RECHECK XI';if(p.strongest_signal==='DOUBT')return'CHECK LATEST';if(p.strongest_signal==='MINUTES')return'MINUTES WATCH';if(p.strongest_signal==='FIT')return'REASSURING';return'MONITOR'}
function render(){const host=q('#dc-team-view');if(!host||!data)return;host.querySelector('[data-press-conference-watch]')?.remove();const rows=(data.players||[]).slice(0,6),sec=document.createElement('section');sec.className='pc106-card';sec.dataset.pressConferenceWatch='1';const active=!!data.friday_press_window;sec.innerHTML=`<div class="pc106-head"><div><p class="eyebrow">PRESS CONFERENCE WATCH · GW${esc(data.next_gw||'—')}</p><h3>${active?'Friday team-news window active':'Team-news monitor'}</h3><p>${active?'Refreshing every 15 minutes through the main press-conference window.':'Latest manager/team-news evidence for your squad.'}</p></div><span class="${active?'live':''}">${active?'LIVE':'WATCH'}</span></div><div class="pc106-meta"><span>${esc(data.article_count||0)} articles scanned</span><span>updated ${esc(age(data.generated_at_utc))}</span>${data.hours_to_deadline!=null?`<span>${esc(data.hours_to_deadline)}h to deadline</span>`:''}</div>${rows.length?`<div class="pc106-list">${rows.map(p=>`<details class="pc106-row ${tone(p.strongest_signal)}"><summary><div><strong>${esc(p.player||'—')}</strong><span>${esc(p.club||'')} · ${esc(p.source_count||0)} source${p.source_count===1?'':'s'}</span></div><div class="pc106-side"><b>${esc(p.strongest_signal||'INFO')}</b><em>${esc(p.confidence||0)}%</em></div></summary><div class="pc106-detail"><strong>${esc(action(p))}</strong><p>${p.strongest_signal==='OUT'?'Press-conference evidence challenges current availability. Re-run XI/captaincy check before lock.':p.strongest_signal==='DOUBT'?'Availability is unresolved. Treat current XI place as provisional until a newer source confirms status.':p.strongest_signal==='MINUTES'?'Manager/team-news discussion raises a minutes/rotation question. Do not automatically bench, but reduce confidence until clarified.':p.strongest_signal==='FIT'?'Recent evidence supports availability; retain normal model weighting unless contradicted.':'Monitor for a clearer signal.'}</p>${(p.articles||[]).slice(0,2).map(a=>`<div class="pc106-source"><span>${esc(a.source||'')}</span><small>${esc(a.title||'')}</small></div>`).join('')}</div></details>`).join('')}</div>`:`<div class="pc106-clear"><strong>No squad press-conference flag yet.</strong><span>The watcher will surface a player only when public manager/team-news language matches a current squad member.</span></div>`}<p class="pc106-note">Evidence layer only: press-conference signals challenge availability/minutes confidence but do not silently override the authoritative XI. Strong changes should appear here before you lock XI/C/VC/TC.</p>`;
 const delta=q('[data-pick-team-delta]',host),auth=q('[data-authoritative-pick-team]',host);if(delta)delta.insertAdjacentElement('afterend',sec);else if(auth)auth.insertAdjacentElement('beforebegin',sec);else host.prepend(sec);document.documentElement.dataset.pressConferenceWatchBuild=BUILD}
async function run(){if(loading)return loading;loading=fetch(`${URL}?pc106=${Date.now()}`,{cache:'no-store'}).then(r=>r.ok?r.json():null).then(x=>{if(x?.status==='SUCCESS'){data=x;render()}}).catch(()=>{}).finally(()=>loading=null);return loading}
function bind(){q('#decision-nav button[data-view="team"]')?.addEventListener('click',()=>setTimeout(run,100),{passive:true});window.addEventListener('fplViewSettled',e=>{if(e.detail?.viewName==='team')setTimeout(run,80)},{passive:true});window.addEventListener('fplCoreDataReady',()=>{if(q('.dashboard-view.active')?.id==='view-team')setTimeout(run,180)},{passive:true});if(q('.dashboard-view.active')?.id==='view-team')run()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();
