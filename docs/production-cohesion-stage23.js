(()=>{
const BUILD='production-cohesion-stage23-20260828-2207';
const KEY='fplWorkingPlanV2';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[c]));
function plan(){try{return JSON.parse(localStorage.getItem(KEY)||'null')}catch{return null}}
function move(){return plan()?.moves?.[0]||null}
function activeTransfers(){return Array.isArray(window.FPLCoreData?.current_squad_transfers)?window.FPLCoreData.current_squad_transfers:[]}
function playerName(id){const d=window.FPLCoreData||{},rows=[...(d.squad||[]),...(d.current_squad_next5||[]),...(d.squad_next5||[])];return rows.find(p=>Number(p.player_id)===Number(id))?.player||null}
function renderPlanStatus(){document.getElementById('working-choice-strip')?.remove();const top=document.querySelector('.topbar');if(!top)return;let chip=document.getElementById('working-plan-chip');const tx=activeTransfers();if(!tx.length){chip?.remove();return}if(!chip){chip=document.createElement('div');chip.id='working-plan-chip';const gw=document.getElementById('gw-pill');if(gw?.parentElement)gw.insertAdjacentElement('afterend',chip);else top.appendChild(chip)}const labels=tx.map(t=>{const out=playerName(t.element_out),inn=playerName(t.element_in);return out&&inn?`${out} → ${inn}`:null}).filter(Boolean);chip.className='working-plan-chip active';if(tx.length===1&&labels[0])chip.innerHTML=`<span>ACTIVE</span><strong>${esc(labels[0])}</strong>`;else chip.innerHTML=`<span>ACTIVE</span><strong>${tx.length} transfer${tx.length===1?'':'s'}</strong>`;chip.title=labels.length?labels.join(' · '):`${tx.length} confirmed transfer${tx.length===1?'':'s'} for the upcoming gameweek.`}
function labelSelectedIncoming(){const m=move();document.querySelectorAll('.pitch-player-card').forEach(card=>card.classList.remove('working-in'));if(!m?.in?.player)return;document.querySelectorAll('.pitch-player-card .pitch-name').forEach(name=>{const txt=(name.textContent||'').replace(/^[CV]\s*/,'').trim().toLowerCase();if(txt.includes(String(m.in.player).toLowerCase()))name.closest('.pitch-player-card')?.classList.add('working-in')})}
function normalizeHeadings(){document.querySelectorAll('.dc-card,.pitch-panel,.pitch-impact,.pitch-bench-panel,.history-panel').forEach(sec=>{const eye=sec.querySelector(':scope > .eyebrow, :scope > div > .eyebrow, .panel-head .eyebrow, .pitch-head .eyebrow');if(eye)eye.classList.add('product-kicker')});}
function activeTabHint(){const active=document.querySelector('#decision-nav button.active');document.querySelectorAll('#decision-nav button').forEach(b=>b.setAttribute('aria-current',b===active?'page':'false'))}
function polish(){renderPlanStatus();labelSelectedIncoming();normalizeHeadings();activeTabHint();document.documentElement.dataset.productionCohesionBuild=BUILD}
function bind(){document.querySelectorAll('#decision-nav button').forEach(b=>b.addEventListener('click',()=>{setTimeout(polish,140);setTimeout(polish,650)},{passive:true}));window.addEventListener('fplSafePlanUpdated',()=>setTimeout(polish,80));window.addEventListener('fplCoreDataReady',()=>setTimeout(polish,20));polish();setTimeout(polish,900);setTimeout(polish,2000)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();