(()=>{
const BUILD='journal-standalone-v1-20260826-1800';
const BASE='https://raw.githubusercontent.com/tlaw77/fpl/main/data/';
const PLAN_KEY='fplWorkingPlanV2';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
async function json(name){try{const r=await fetch(`${BASE}${name}?v=${BUILD}`,{cache:'no-store'});return r.ok?await r.json():null}catch{return null}}
function plan(){try{return JSON.parse(localStorage.getItem(PLAN_KEY)||'null')}catch{return null}}
function fmtWhen(x){if(!x)return'';try{return new Date(x).toLocaleString([],{day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'})}catch{return x}}
function key(event,outId,inId,outName,inName){return `${event||''}:${outId||outName||''}>${inId||inName||''}`}
function sameRoute(rec,t){const ri=rec?.in_player_id,ro=rec?.out_player_id,ti=t?.element_in,to=t?.element_out;if(ri!=null&&ti!=null&&Number(ri)!==Number(ti))return false;if(ro!=null&&to!=null&&Number(ro)!==Number(to))return false;return String(rec?.out||'').trim().toLowerCase()===String(t?.out_name||t?.out||'').trim().toLowerCase()&&String(rec?.in||'').trim().toLowerCase()===String(t?.in_name||t?.in||'').trim().toLowerCase()}
function priorContext(event,t,rh){const cutoff=t?.time||t?.committed_at;let best=null;for(const s of rh?.snapshots||[]){if(Number(s.next_gw)!==Number(event))continue;if(cutoff&&s.captured_at_utc&&new Date(s.captured_at_utc)>new Date(cutoff))continue;const lenses=[];for(const [type,field,label] of [['lower_variance','lower_variance','Lower variance'],['variety','variety','Leverage']]){const r=(s[field]||[]).find(x=>sameRoute(x,t));if(r)lenses.push({type,label,model_uplift:r.gain,target_rival_ownership_pct:r.target_rival_ownership_pct,next3:r.next3||[],rationale:r.rationale||[]})}if(lenses.length){const at=new Date(s.captured_at_utc||0).getTime();if(!best||at>best.at)best={at,snapshot_at_utc:s.captured_at_utc,team_rank_at_snapshot:s.team_rank,bank_at_snapshot:s.bank,lenses}}}return best?({...best,at:undefined}):null}
const card='background:linear-gradient(180deg,rgba(24,43,72,.96),rgba(20,36,61,.96));border:1px dashed rgba(250,204,21,.46);border-radius:16px;padding:14px;margin:8px 0 10px;overflow:hidden';
const metric='min-width:0;padding:9px 10px;border:1px solid rgba(148,163,184,.18);border-radius:11px;background:rgba(7,20,39,.30)';
function metricBox(label,value){return `<div data-journal-metric="1" style="${metric}"><span style="display:block;font-size:7px;line-height:1.2;color:#aebbd0;font-weight:750;margin-bottom:4px">${esc(label)}</span><strong style="display:block;font-size:14px;line-height:1;color:#f8fafc;font-weight:900;letter-spacing:-.02em">${esc(value)}</strong></div>`}
function fdrColor(n){n=Number(n);return n<=2?'#22c55e':n===3?'#64748b':n===4?'#f59e0b':'#ef4444'}
function evidence(ctx){if(!ctx?.lenses?.length)return '<div style="font-size:8px;color:#9fb0c8;margin-top:8px">No exact prior model recommendation captured for this route.</div>';const low=ctx.lenses.find(x=>x.type==='lower_variance');const lev=ctx.lenses.find(x=>x.type==='variety');const base=lev||low||ctx.lenses[0];const own=base?.target_rival_ownership_pct;const fs=base?.next3||[];const metrics=[low?.model_uplift!=null?metricBox('Lower-variance uplift',Number(low.model_uplift).toFixed(1)):'',lev?.model_uplift!=null?metricBox('Leverage uplift',Number(lev.model_uplift).toFixed(1)):'',own!=null?metricBox('Nearest-rival ownership',`${Number(own).toFixed(0)}%`):'',ctx.bank_at_snapshot!=null?metricBox('Bank then',`£${Number(ctx.bank_at_snapshot).toFixed(1)}m`):''].join('');const fixtures=fs.map(f=>`<div style="display:flex;align-items:center;gap:5px;min-width:0;padding:6px 7px;border-radius:9px;background:rgba(255,255,255,.035);font-size:8px;line-height:1.2;color:#e5edf8"><i style="display:inline-block;flex:0 0 8px;width:8px;height:8px;border-radius:999px;background:${fdrColor(f.fdr)}"></i><span style="min-width:0;overflow-wrap:anywhere">${esc(f.opponent)} ${esc(f.venue)}</span></div>`).join('');return `<div style="margin-top:11px;padding-top:11px;border-top:1px solid rgba(255,255,255,.08)"><div style="display:flex;align-items:flex-start;justify-content:space-between;gap:10px;margin-bottom:8px"><strong style="font-size:9px;font-weight:850;color:#e2e8f0">Prior model context</strong><span style="font-size:8px;line-height:1.3;color:#9fb0c8;text-align:right">${ctx.snapshot_at_utc?fmtWhen(ctx.snapshot_at_utc):'Captured before decision'}${ctx.team_rank_at_snapshot?` · rank ${ctx.team_rank_at_snapshot}`:''}</span></div><div data-journal-grid="1" style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:7px">${metrics}</div>${fs.length?`<div style="margin-top:9px;padding-top:9px;border-top:1px solid rgba(255,255,255,.07)"><b style="display:block;font-size:8px;color:#dce9fb;font-weight:850;margin-bottom:7px">Fixture case</b><div style="display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:6px">${fixtures}</div></div>`:''}<div style="margin-top:9px;padding-top:8px;border-top:1px solid rgba(255,255,255,.07)"><strong style="font-size:8px;color:#dce9fb;font-weight:850">Why it was considered</strong></div><div style="font-size:7px;color:#9fb0c8;margin-top:6px">Historical dashboard heuristic · not projected FPL points.</div></div>`}
function transferCard(t,pending,ctx){return `<div data-journal-card="1" style="${card}"><div style="display:flex;align-items:flex-start;justify-content:space-between;gap:10px"><div style="min-width:0"><strong style="display:block;font-size:13px;line-height:1.25;font-weight:850;color:#f8fafc">${esc(t.out_name||t.out||'—')} → ${esc(t.in_name||t.in||'—')}</strong><div style="margin-top:5px;font-size:9px;color:#9fb0c8">${t.time?fmtWhen(t.time):pending&&t.committed_at?fmtWhen(t.committed_at):''}</div></div><span style="display:inline-flex;align-items:center;padding:6px 9px;border-radius:999px;background:rgba(250,204,21,.12);border:1px solid rgba(250,204,21,.18);color:#fde68a;font-size:8px;line-height:1;font-weight:850;white-space:nowrap">${pending?'Pending · committed here':'Confirmed'}</span></div>${evidence(ctx)}</div>`}
function weekCard(w,pending,rh){const transfers=[...(w.transfers||[])];const officialKeys=new Set(transfers.map(t=>key(w.event,t.element_out,t.element_in,t.out_name,t.in_name)));const extras=(pending||[]).filter(t=>!officialKeys.has(key(w.event,t.element_out,t.element_in,t.out_name,t.in_name)));const hasTx=transfers.length||extras.length;return `<details class="history-snapshot" ${extras.length?'open':''}><summary><span>GW${w.event}</span><small>${hasTx?`${transfers.length+extras.length} transfer${transfers.length+extras.length===1?'':'s'} · `:''}${w.captain_name?`C ${esc(w.captain_name)}`:'No captain record'}</small></summary>${hasTx?`<div style="display:grid;grid-template-columns:1fr;gap:8px;margin-top:8px">${transfers.map(t=>transferCard(t,false,t.recommendation_context||priorContext(w.event,t,rh))).join('')}${extras.map(t=>transferCard(t,true,priorContext(w.event,t,rh))).join('')}</div>`:'<p class="subtle">No transfer made this gameweek.</p>'}</details>`}
function ensureHost(){const intel=document.getElementById('view-intel');if(!intel)return null;let host=document.getElementById('decision-history-panel');if(!host){host=document.createElement('div');host.id='decision-history-panel';const first=intel.querySelector('.panel');if(first?.nextSibling)intel.insertBefore(host,first.nextSibling);else intel.prepend(host)}return host}
async function render(){const el=ensureHost();if(!el)return;el.dataset.journalStandaloneBuild=BUILD;const [hist,latest,rh]=await Promise.all([json('decision_history.json'),json('latest.json'),json('recommendation_history.json')]);const weeks=[...(hist?.weeks||[])];const p=plan();const nextGw=Number(latest?.next_gw||0);const pending=[];for(const m of p?.moves||[])pending.push({event:nextGw,element_out:m.out?.player_id,out_name:m.out?.player,element_in:m.in?.player_id,in_name:m.in?.player,committed_at:m.committed_at});if(pending.length&&!weeks.some(w=>Number(w.event)===nextGw))weeks.push({event:nextGw,captain_name:null,transfers:[]});weeks.sort((a,b)=>Number(b.event)-Number(a.event));el.innerHTML=`<section class="history-panel"><div class="panel-head"><div><p class="eyebrow">DECISION JOURNAL</p><h2>Decisions actually made</h2></div><div class="subtle">Decision first · model evidence when captured</div></div>${weeks.length?weeks.map(w=>weekCard(w,pending.filter(t=>Number(t.event)===Number(w.event)),rh)).join(''):'<p class="subtle">No recorded decisions yet.</p>'}</section>`}
function boot(){render();setTimeout(render,800);setTimeout(render,2500);window.addEventListener('fplPlanChanged',()=>setTimeout(render,100))}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
window.FPLJournalStandalone={build:BUILD,render};
})();