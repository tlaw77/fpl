const EO_DATA_URL='https://raw.githubusercontent.com/tlaw77/fpl/main/data/latest.json';
const eoEsc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function eoBand(eo,inSquad){if(inSquad){if(eo>=75)return['shield','Shield'];if(eo>=40)return['neutral','Neutral'];return['leverage','Leverage'];}if(eo>=75)return['major-danger','Major danger'];if(eo>=40)return['danger','Danger'];return['different-against','Differential against'];}
function activeSquad(d){const base=d.current_squad_next5||d.squad_next5||[];const plan=window.FPLPlan?.reconcile?.(d);return window.FPLPlan?.apply?.(base,plan)||[...base];}
function exposureMap(d){return new Map((d.player_exposure||[]).map(x=>[x.player_id,x]));}
function strategyPosture(d){const rank=Number(d.me?.rank||1), n=Number(d.league?.manager_count||1), gw=Number(d.current_gw||1);const pct=(rank-1)/Math.max(1,n-1);if(gw<=5){if(pct>.65)return['Controlled chase','You are behind in the table, but it is still very early. Keep the strongest high-EO shields and create only 2–3 deliberate leverage points.'];return['Balanced early-season','Do not chase variance yet. Protect against the strongest high-EO players and let fixtures/form identify where to differentiate.'];}if(pct>.7)return['Chase','You need more routes to gain: retain only essential shields and increase exposure to strong low-EO starters/captains.'];if(pct<.3)return['Protect lead','Reduce unnecessary variance: cover the strongest rival-owned players and use leverage mainly where the expected upside is clearly superior.'];return['Balanced+','Use a safe core with selective leverage, increasing or reducing risk according to the gap and remaining gameweeks.'];}
function renderEOStrategy(d){const el=document.querySelector('#eo-strategy');if(!el)return;const squad=activeSquad(d);const exp=exposureMap(d);const enriched=squad.map(p=>{const x=exp.get(p.player_id)||{};const eo=Number(x.effective_ownership_pct||0);const band=eoBand(eo,true);return {...p,eo,own:Number(x.ownership_pct||0),classification:band[0],label:band[1],mult:Number(x.my_multiplier??(p.captain?2:1))};});
const shields=enriched.filter(x=>x.eo>=75).sort((a,b)=>b.eo-a.eo);const leverage=enriched.filter(x=>x.eo<40&&x.position!=='GKP').sort((a,b)=>a.eo-b.eo);const squadIds=new Set(squad.map(x=>x.player_id));const dangers=(d.player_exposure||[]).filter(x=>!squadIds.has(x.player_id)&&Number(x.effective_ownership_pct||0)>=40).sort((a,b)=>Number(b.effective_ownership_pct)-Number(a.effective_ownership_pct)).slice(0,4);
const [posture,postureText]=strategyPosture(d);const shieldText=shields.length?`${shields.slice(0,3).map(x=>`${x.player} ${x.eo.toFixed(1)}% EO`).join(', ')}. These are the players most capable of hurting you when they return, so benching/selling/captain-opposing them needs a strong reason.`:'You currently have no player above the 75.0% EO shield threshold.';
const levText=leverage.length?`${leverage.slice(0,4).map(x=>`${x.player} ${x.eo.toFixed(1)}% EO`).join(', ')}. These are your cleanest routes to gain when they start and return.`:'Your current XI has little low-EO leverage; most of your outcome will track the league.';
const dangerText=dangers.length?`${dangers.map(x=>`${x.player} ${Number(x.effective_ownership_pct).toFixed(1)}% EO`).join(', ')}. Missing these is where a good score can still become a red-arrow style outcome inside the mini-league.`:'There is no major 40.0%+ EO player currently outside the working squad.';
el.innerHTML=`<section class="eo-panel"><div class="eo-head"><div><p class="eyebrow">HOW TO PLAY THIS SQUAD</p><h2>${eoEsc(posture)}</h2></div><span class="eo-position">Rank ${d.me?.rank}/${d.league?.manager_count} · GW${d.current_gw}</span></div><p class="eo-summary">${eoEsc(postureText)}</p><div class="eo-grid"><article><span class="eo-tag shield">Shield</span><strong>Protect downside</strong><p>${eoEsc(shieldText)}</p></article><article><span class="eo-tag leverage">Leverage</span><strong>Create routes to gain</strong><p>${eoEsc(levText)}</p></article><article><span class="eo-tag danger">Danger</span><strong>Know what can hurt</strong><p>${eoEsc(dangerText)}</p></article></div><div class="eo-rule"><strong>Decision rule:</strong> high EO + strong fixture = usually cover/protect; low EO + strong role/fixture = leverage; low EO by itself is not a reason to own someone. Captaincy magnifies the choice, so take captain leverage only when the underlying pick is close enough on expected outcome.</div></section>`;}
async function bootEOStrategy(){try{const r=await fetch(`${EO_DATA_URL}?t=${Date.now()}`,{cache:'no-store'});if(!r.ok)return;renderEOStrategy(await r.json())}catch(e){console.warn('eo strategy',e)}}
function loadDecisionJournal(){const transferView=document.querySelector('#view-transfer');if(transferView&&!document.querySelector('#decision-history-panel')){const el=document.createElement('div');el.id='decision-history-panel';transferView.appendChild(el)}if(!document.querySelector('link[data-decision-history]')){const l=document.createElement('link');l.rel='stylesheet';l.href='decision-history.css';l.dataset.decisionHistory='1';document.head.appendChild(l)}if(!document.querySelector('script[data-decision-history]')){const s=document.createElement('script');s.src='decision-history.js';s.dataset.decisionHistory='1';document.body.appendChild(s)}}
setTimeout(bootEOStrategy,650);setTimeout(loadDecisionJournal,300);window.addEventListener('fplPlanChanged',()=>setTimeout(bootEOStrategy,20));window.addEventListener('effectiveSquadRendered',e=>{if(e.detail)setTimeout(()=>renderEOStrategy(e.detail),30)});
