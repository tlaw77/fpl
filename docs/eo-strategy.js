const EO_DATA_URL='https://raw.githubusercontent.com/tlaw77/fpl/main/data/latest.json';
const eoEsc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function activeSquad(d){const base=d.current_squad_next5||d.squad_next5||[];const plan=window.FPLPlan?.reconcile?.(d);return window.FPLPlan?.apply?.(base,plan)||[...base];}
function exposureMap(d){return window.FPLExposureModel?.exposureMap(d)||new Map((d.player_exposure||[]).map(x=>[x.player_id,x]));}
function targetExposureMap(d){const targetIds=new Set((d.target_rivals||[]).map(x=>x.entry_id));const rivals=(d.rivals||[]).filter(r=>targetIds.has(r.entry_id));const m=new Map();for(const r of rivals){for(const p of r.picks||[]){const cur=m.get(p.player_id)||{owned:0,started:0,captained:0};cur.owned++;if((p.slot||99)<=11)cur.started++;if(p.captain)cur.captained++;m.set(p.player_id,cur)}}return {map:m,n:rivals.length};}
function strategyPosture(d){const rank=Number(d.me?.rank||1),n=Number(d.league?.manager_count||1),gw=Number(d.current_gw||1),pct=(rank-1)/Math.max(1,n-1);if(gw<=5){if(pct>.65)return['Controlled chase','You are behind in the table, but it is still very early. Keep the strongest high-EO shields and create only 2–3 deliberate leverage points where the football case is strong.'];return['Balanced early-season','Do not chase variance yet. Protect against the strongest high-EO players and let fixtures/form identify where to differentiate.'];}if(pct>.7)return['Chase','You need more routes to gain: retain essential shields and increase exposure to strong low-EO starters/captains.'];if(pct<.3)return['Protect lead','Reduce unnecessary variance: cover the strongest rival-owned players and use leverage mainly where expected upside is clearly superior.'];return['Balanced+','Use a safe core with selective leverage, increasing or reducing risk according to the gap and remaining gameweeks.'];}
function stylePill(r){return `<span class="eo-style-pill eo-style-${r.score>80?'vhigh':r.score>65?'high':r.score>45?'mid':'low'}"><b>${r.score}</b><small>${eoEsc(r.band)}</small></span>`}
function renderLeagueStyles(d){const M=window.FPLExposureModel;if(!M)return;const intel=document.getElementById('view-intel');if(!intel)return;let host=document.getElementById('league-style-ratings');if(!host){host=document.createElement('section');host.id='league-style-ratings';host.className='panel eo-league-style';const standings=intel.querySelector('#rivals-table')?.closest('.panel');if(standings)intel.insertBefore(host,standings);else intel.appendChild(host)}const exp=exposureMap(d);const managers=[{...(d.me||{}),picks:d.squad||[]},...(d.rivals||[])].map(m=>({...m,style:M.styleRating(m.picks||[],exp)})).sort((a,b)=>b.style.score-a.style.score||b.style.leverage-a.style.leverage);const mine=managers.find(x=>x.entry_id===d.me?.entry_id);const avg=managers.length?Math.round(managers.reduce((s,x)=>s+x.style.score,0)/managers.length):0;host.innerHTML=`<div class="panel-head"><div><p class="eyebrow">SQUAD STYLE</p><h2>How everyone is playing the league</h2></div><div class="subtle">Last locked deadline · starting XI only · 0 protective → 100 high variance</div></div><div class="eo-style-summary">You: <strong>${mine?.style.score??'—'} · ${eoEsc(mine?.style.band||'—')}</strong> · league avg ${avg}</div><div class="eo-style-list">${managers.map((m,i)=>`<div class="eo-style-row ${m.entry_id===d.me?.entry_id?'you':''}"><span class="eo-style-rank">${i+1}</span><div><strong>${eoEsc(m.manager||m.team_name||'Manager')}</strong><small>${eoEsc(m.team_name||'')} · rank ${m.rank||'—'} · ${m.style.leverage}/11 leverage · ${m.style.shields} shields · avg EO ${m.style.avgEo}%</small></div>${stylePill(m.style)}</div>`).join('')}</div><div class="eo-rule"><strong>How to read it:</strong> deterministic variance/style score, not team quality. The biggest driver is how many of the starting XI are genuinely below 40% mini-league EO. League-wide Shield/Neutral/Leverage definitions are identical for every manager; bench players are excluded. Captain uniqueness and degree of differentiation then refine the score.</div>`;}
function renderEOStrategy(d){const M=window.FPLExposureModel,el=document.querySelector('#eo-strategy');if(!M||!el)return;const squad=activeSquad(d),exp=exposureMap(d),targ=targetExposureMap(d),xi=M.startingXI(squad),xiIds=new Set(xi.map(x=>x.player_id));const enriched=squad.map(p=>{const active=xiIds.has(p.player_id),role=M.roleFor(p,exp,active),tx=targ.map.get(p.player_id)||{owned:0,started:0,captained:0};return {...p,eo:role.eo,classification:role.key,label:role.label,family:role.family,active,targetOwned:tx.owned,targetStarted:tx.started,targetCaptained:tx.captained};});const activeRows=enriched.filter(x=>x.active),rating=M.styleRating(xi,exp),shields=activeRows.filter(x=>x.family==='shield').sort((a,b)=>b.eo-a.eo),leverage=activeRows.filter(x=>x.family==='leverage').sort((a,b)=>a.eo-b.eo),squadIds=new Set(squad.map(x=>x.player_id)),dangers=(d.player_exposure||[]).filter(x=>!squadIds.has(x.player_id)&&Number(x.effective_ownership_pct||0)>=40).sort((a,b)=>Number(b.effective_ownership_pct)-Number(a.effective_ownership_pct)).slice(0,4),targetUnique=activeRows.filter(x=>targ.n&&x.targetStarted===0),targetCovered=activeRows.filter(x=>targ.n&&x.targetStarted===targ.n);const [posture,postureText]=strategyPosture(d);const shieldText=shields.length?`${shields.slice(0,3).map(x=>`${x.player} ${x.eo.toFixed(1)}% EO`).join(', ')}. These reduce downside because most of the league is exposed when they return.`:'No active player is above the 75% EO shield threshold.';const levText=leverage.length?`${leverage.length}/${activeRows.length} active players are below 40% EO: ${leverage.slice(0,5).map(x=>`${x.player} ${x.eo.toFixed(1)}%`).join(', ')}. These are genuine league-wide leverage routes.`:'The active XI has little low-EO leverage; most outcomes will track the league.';const dangerText=dangers.length?`${dangers.map(x=>`${x.player} ${Number(x.effective_ownership_pct).toFixed(1)}% EO`).join(', ')}. These are the main high-EO players outside your squad that can create downside.`:'There is no major 40%+ EO player currently outside the working squad.';const targetText=targ.n?`${targetUnique.length} active picks are started by none of your ${targ.n} nearest target rivals${targetUnique.length?`: ${targetUnique.slice(0,5).map(x=>x.player).join(', ')}`:''}. ${targetCovered.length} are fully covered by all ${targ.n}. This is direct-rival leverage, separate from the league-wide label.`:'Target-rival picks are not available in this snapshot.';const riskText=rating.score>80?'Very differentiated: large upside when your calls land, but substantial downside in a strong template week.':rating.score>65?'Aggressive blend: several routes to gain, with meaningful weekly volatility.':rating.score>45?'Controlled leverage: differentiated enough to move rank without abandoning the core.':'Relatively protective: more of your weekly outcome will move with the league.';el.innerHTML=`<section class="eo-panel"><div class="eo-head"><div><p class="eyebrow">HOW TO PLAY THIS SQUAD</p><h2>${eoEsc(posture)}</h2></div><span class="eo-position">Rank ${d.me?.rank}/${d.league?.manager_count} · GW${d.current_gw}</span></div><p class="eo-summary">${eoEsc(postureText)}</p><div class="eo-rating"><div class="eo-rating-score">${rating.score}<span>/100</span></div><div><strong>${eoEsc(rating.band)}</strong><p>${eoEsc(riskText)}</p><small>${rating.leverage}/${activeRows.length} leverage · ${rating.shields} shields · avg XI EO ${rating.avgEo}% · captain ${eoEsc(rating.captain)}</small></div></div><div class="eo-grid"><article><span class="eo-tag shield">Shield</span><strong>Protect downside</strong><p>${eoEsc(shieldText)}</p></article><article><span class="eo-tag leverage">Leverage</span><strong>Create routes to gain</strong><p>${eoEsc(levText)}</p></article><article><span class="eo-tag danger">Danger</span><strong>Know what can hurt</strong><p>${eoEsc(dangerText)}</p></article></div><div class="eo-target"><strong>Versus nearest rivals:</strong> ${eoEsc(targetText)}</div><div class="eo-rule"><strong>Canonical rule:</strong> active player EO ≥75% = Shield; 40–74.9% = Neutral; below 40% = Leverage. A benched player is a bench differential, not leverage. Direct-rival uniqueness is shown separately and never changes the league-wide role label.</div></section>`;renderLeagueStyles(d);}
async function bootEOStrategy(){try{const r=await fetch(`${EO_DATA_URL}?t=${Date.now()}`,{cache:'no-store'});if(!r.ok)return;const d=await r.json();window.FPLExposureModel?.normalizeExposure(d);renderEOStrategy(d)}catch(e){console.warn('eo strategy',e)}}
function loadDecisionJournal(){const transferView=document.querySelector('#view-transfer');if(transferView&&!document.querySelector('#decision-history-panel')){const el=document.createElement('div');el.id='decision-history-panel';transferView.appendChild(el)}if(!document.querySelector('link[data-decision-history]')){const l=document.createElement('link');l.rel='stylesheet';l.href='decision-history.css';l.dataset.decisionHistory='1';document.head.appendChild(l)}if(!document.querySelector('script[data-decision-history]')){const s=document.createElement('script');s.src='decision-history.js';s.dataset.decisionHistory='1';document.body.appendChild(s)}}
setTimeout(bootEOStrategy,650);setTimeout(loadDecisionJournal,300);window.addEventListener('fplPlanChanged',()=>setTimeout(bootEOStrategy,20));window.addEventListener('effectiveSquadRendered',e=>{if(e.detail){window.FPLExposureModel?.normalizeExposure(e.detail);setTimeout(()=>renderEOStrategy(e.detail),30)}});
