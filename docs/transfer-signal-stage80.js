(()=>{
const BUILD='transfer-signal-stage80-20260907-2';
const BASE='https://raw.githubusercontent.com/tlaw77/fpl/main/data/';
const q=(s,r=document)=>r.querySelector(s);
let synthesis=null,history=null,loading=false;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const n=(v,d=0)=>Number.isFinite(Number(v))?Number(v):d;
async function get(name){const r=await fetch(`${BASE}${name}?signal80=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(`${name} ${r.status}`);return r.json()}
function gateState(x){const edge=n(x.edge_over_hold_6gw),edgeNeed=n(x.required_edge),support=n(x.leader_support_models),supportNeed=n(x.required_support_models),edgeClear=edgeNeed>0&&edge>=edgeNeed,supportClear=supportNeed>0&&support>=supportNeed;return{edge,edgeNeed,support,supportNeed,edgeClear,supportClear,clear:edgeClear&&supportClear,clearCount:Number(edgeClear)+Number(supportClear)}}
function scoreFrom(syn){const r=syn?.robustness||{},edge=n(r.single_step_edge_over_hold_6gw),req=n(r.required_edge),support=n(r.measured_leader_support_models),supportReq=n(r.required_consensus_models),clears=!!r.transfer_clears_gate;if(clears)return 100;const e=req>0?Math.max(0,Math.min(1,edge/req)):0,c=supportReq>0?Math.max(0,Math.min(1,support/supportReq)):0;return Math.min(99,Math.round((e*72+c*28)*10)/10)}
function band(score,clears){if(clears)return'act';if(score>=65)return'watch';return'stable'}
function labelFor(cur){const g=gateState(cur),action=String(cur.action||'HOLD').toUpperCase();if(g.clear)return action==='TRANSFER'?'ACT · TRANSFER':'ACTION CHECK · GATE CLEARED';if(cur.signal_band==='WATCH')return action==='HOLD'?'HOLD · CHALLENGER RISING':'WATCH · DECISION MOVING';return action==='HOLD'?'HOLD · GATE INTACT':'PLAN · STABLE'}
function currentSnapshot(){const r=synthesis?.robustness||{},a=synthesis?.current_action||{},score=scoreFrom(synthesis),clears=!!r.transfer_clears_gate,b=band(score,clears);return{captured_at_utc:synthesis?.generated_at_utc,next_gw:synthesis?.next_gw,action:a.action||'HOLD',leader:r.single_step_leader||'—',edge_over_hold_6gw:n(r.single_step_edge_over_hold_6gw),required_edge:n(r.required_edge),leader_support_models:n(r.measured_leader_support_models),required_support_models:n(r.required_consensus_models),transfer_clears_gate:clears,signal_score:score,signal_band:b.toUpperCase()}}
function snapshots(){const cur=currentSnapshot(),arr=(history?.snapshots||[]).filter(x=>Number(x.next_gw)===Number(cur.next_gw));if(!arr.length)return[cur];const last=arr[arr.length-1];if(Math.abs(n(last.signal_score)-cur.signal_score)>.01||last.action!==cur.action||!!last.transfer_clears_gate!==!!cur.transfer_clears_gate)arr.push(cur);return arr.slice(-10)}
function trend(rows,action){if(rows.length<2)return{key:'flat',label:'BUILDING TREND',text:'Baseline captured · direction will appear after the next meaningful model change.'};const prev=rows[rows.length-2],cur=rows[rows.length-1],delta=n(cur.signal_score)-n(prev.signal_score),hold=String(action).toUpperCase()==='HOLD';if(Math.abs(delta)<1)return{key:'flat',label:'STABLE',text:'No meaningful movement since the previous decision snapshot.'};const towardTransfer=delta>0;return{key:towardTransfer?'up':'down',label:towardTransfer?'TOWARD TRANSFER':'TOWARD HOLD',text:`${hold?(towardTransfer?'HOLD weakening':'HOLD strengthening'):(towardTransfer?'Transfer case strengthening':'Transfer case weakening')} · ${delta>0?'+':''}${delta.toFixed(1)} signal points since the last change.`}}
function changeDetail(rows){if(rows.length<2)return'No prior meaningful signal snapshot yet.';const prev=rows[rows.length-2],cur=rows[rows.length-1],parts=[],de=n(cur.edge_over_hold_6gw)-n(prev.edge_over_hold_6gw),ds=n(cur.leader_support_models)-n(prev.leader_support_models);if(Math.abs(de)>=.05)parts.push(`projected edge ${de>0?'rose':'fell'} ${Math.abs(de).toFixed(1)} pts`);else parts.push('projected edge was unchanged');if(ds)parts.push(`model agreement ${ds>0?'rose':'fell'} from ${n(prev.leader_support_models)}/${Math.max(1,n(prev.required_support_models))} to ${n(cur.leader_support_models)}/${Math.max(1,n(cur.required_support_models))}`);else parts.push(`model agreement stayed ${n(cur.leader_support_models)}/${Math.max(1,n(cur.required_support_models))}`);if(String(prev.leader||'')!==String(cur.leader||''))parts.push(`leading route changed from ${prev.leader||'—'} to ${cur.leader||'—'}`);else parts.push(`leading route stayed ${cur.leader||'—'}`);return parts.join(' · ')+'.'}
function transition(rows){const cur=gateState(rows[rows.length-1]||{}),prev=rows.length>1?gateState(rows[rows.length-2]):null;if(cur.clear&&!prev?.clear)return'crossed';if(!cur.clear&&prev?.clear)return'lost';return cur.clear?'clear':'blocked'}
function timeLabel(x){try{return new Date(x).toLocaleString([],{weekday:'short',hour:'2-digit',minute:'2-digit'})}catch{return''}}
function gateHtml(cur,rows){const g=gateState(cur),state=transition(rows),edgeMargin=g.edge-g.edgeNeed,supportGap=Math.max(0,g.supportNeed-g.support);let title=`${g.clearCount}/2 ACTION GATES CLEAR`,copy=`${2-g.clearCount} gate${2-g.clearCount===1?'':'s'} still block the transfer.`;if(state==='crossed'){title='ACTION THRESHOLD CROSSED NOW';copy='Both required gates have just cleared. Run the final availability, price and deadline checks, then act on the authoritative route.'}else if(state==='clear'){title='ACTION THRESHOLD CLEARED';copy='Both required gates remain clear. Complete final safety checks before execution.'}else if(state==='lost'){title='ACTION GATE LOST';copy='A previously clear transfer case has fallen back below the full threshold. Reassess before acting.'}return `<div class="txs-trigger ${state}"><div><span>${esc(title)}</span><strong>${esc(g.clear?cur.leader:'NO CALL TO ACTION YET')}</strong><small>${esc(copy)}</small></div><div class="txs-gates"><span class="${g.edgeClear?'ok':'wait'}">${g.edgeClear?'✓':'○'} EDGE ${g.edgeClear?`+${edgeMargin.toFixed(1)} ABOVE`:`${Math.abs(edgeMargin).toFixed(1)} SHORT`}</span><span class="${g.supportClear?'ok':'wait'}">${g.supportClear?'✓':'○'} AGREEMENT ${g.support}/${g.supportNeed}${supportGap?` · NEEDS ${supportGap}`:''}</span></div></div>`}
function detailHtml(cur,rows){const a=synthesis?.current_action||{},g=gateState(cur),remaining=Math.max(0,g.edgeNeed-g.edge),supportGap=Math.max(0,g.supportNeed-g.support),hit=n(a.next_transfer_hit_cost),ft=n(a.free_transfers_remaining),completed=a.completed_transfer?.route||'';const why=g.clear?'The challenger has cleared both projected-points and cross-model agreement gates.':g.edgeClear?'The points gate is clear, but HOLD remains authoritative because cross-model agreement is still short.':g.supportClear?'Model agreement is sufficient, but the projected-points edge is still below its hurdle.':'Neither action gate is currently clear.';const needs=[];if(remaining>0)needs.push(`${remaining.toFixed(1)} more projected points`);if(supportGap)needs.push(`${supportGap} more supporting model${supportGap===1?'':'s'}`);const flip=g.clear?'The full gate is clear; verify team news, price and timing before executing.':`${needs.join(' and ')||'Material new evidence'} must clear before HOLD can be replaced.`;return `<details class="txs-details"><summary><span>Why this signal?</span><small>Tap for evidence</small></summary><div class="txs-detail-body"><div class="txs-detail-grid"><div><span>Closest challenger</span><strong>${esc(cur.leader)}</strong><small>${g.clear?'actionable after safety checks':'not recommended yet'}</small></div><div><span>Evidence pressure</span><strong>${cur.signal_score.toFixed(0)}/100</strong><small>direction, not confidence</small></div><div><span>Points gate</span><strong>${g.edge.toFixed(1)} vs ${g.edgeNeed.toFixed(1)}</strong><small>${g.edgeClear?'cleared':'still blocked'}</small></div><div><span>Agreement gate</span><strong>${g.support}/${g.supportNeed} models</strong><small>${g.supportClear?'cleared':`${supportGap} more needed`}</small></div></div><div class="txs-explain"><p><b>Active decision:</b> ${esc(a.action||'HOLD')}.</p><p><b>Why:</b> ${esc(why)}</p><p><b>What flips it:</b> ${esc(flip)}</p><p><b>What changed:</b> ${esc(changeDetail(rows))}</p>${completed?`<p><b>Already locked:</b> ${esc(completed)}. The signal evaluates whether an <em>additional</em> move is justified.</p>`:''}<p><b>Transfer context:</b> ${ft} free transfer${ft===1?'':'s'} remaining${hit?` · next move costs -${hit}`:' · no hit currently required'}.</p></div><p class="txs-method">The points hurdle alone is not the action threshold. A route replaces HOLD only when both projected edge and cross-model agreement clear together.</p></div></details>`}
function render(){const view=q('#view-transfer');if(!view||!synthesis)return;const phase=q('[data-transfer-phase]',view),decision=q('#gw-decision-brief',view),cur=currentSnapshot(),rows=snapshots(),b=cur.signal_band.toLowerCase(),pct=Math.max(3,Math.min(100,cur.signal_score)),active=String(cur.action).toUpperCase();let sec=q('[data-decision-signal]',view);if(!sec){sec=document.createElement('section');sec.dataset.decisionSignal='1'}sec.className=`tx-signal-card tx-signal-${b}`;sec.innerHTML=`<div class="txs-head"><div><p class="eyebrow">BETWEEN-GAMEWEEK ACTION MONITOR · GW${esc(cur.next_gw)}</p><h3>${esc(labelFor(cur))}</h3></div><div class="txs-light" aria-label="${esc(b)} signal"><i></i><i></i><i></i></div></div><div class="txs-gauge" aria-label="Challenger evidence pressure ${pct.toFixed(0)} out of 100"><div class="txs-zones"><span></span><span></span><span></span></div><b style="left:${pct}%"></b></div>${gateHtml(cur,rows)}<p class="txs-note"><b>${esc(active)} is the active instruction.</b> Trend and evidence below show what would change it.</p>${detailHtml(cur,rows)}`;if(decision&&decision.parentElement===view)decision.insertAdjacentElement('afterend',sec);else if(phase&&phase.parentElement===view)phase.insertAdjacentElement('afterend',sec);else if(view.firstElementChild)view.insertBefore(sec,view.firstElementChild.nextSibling);else view.appendChild(sec);document.documentElement.dataset.transferSignalBuild=BUILD}
async function load(){if(loading)return;loading=true;try{synthesis=window.FPLCoreData?.decision_synthesis||synthesis||await get('decision_synthesis.json');try{history=await get('decision_signal_history.json')}catch{history=null}render()}catch{}finally{loading=false}}
function refresh(){requestAnimationFrame(render)}
function bind(){load();q('#decision-nav button[data-view="transfer"]')?.addEventListener('click',()=>{load();refresh()},{passive:true});window.addEventListener('fplCoreDataReady',e=>{synthesis=(e.detail||window.FPLCoreData)?.decision_synthesis||synthesis;refresh()},{passive:true});window.addEventListener('fplSafePlanUpdated',()=>{synthesis=null;history=null;load()},{passive:true});window.addEventListener('fplViewSettled',e=>{if(e.detail?.viewName==='transfer')refresh()},{passive:true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();
